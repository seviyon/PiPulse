{
  "name": "pipulse",
  "private": true,
  "version": "0.0.0",
  "description": "A modern rewrite of RPi-Monitor.",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=packages/storage && npm run build --workspace=packages/collector && npm run build --workspace=packages/api && npm run build --workspace=packages/web",
    "pretest": "npm run build --workspace=packages/storage && npm run build --workspace=packages/collector",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "dev": "npm run dev --workspace packages/api"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/node": "^22.7.5",
    "eslint": "^9.12.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.8.1",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
